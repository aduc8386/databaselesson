pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
    buildscript {
        dependencies {
            classpath "io.realm:realm-gradle-plugin:10.10.1"
        }
    }
}
rootProject.name = "DataBaseLesson"
include ':app'
